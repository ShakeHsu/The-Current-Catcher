# 🔧 网络故障排查指南

## 错误信息
```
❌ 获取实时行情失败: ('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))
```

## 问题原因

这是**网络连接问题**，AKShare无法连接到东方财富数据源。

## 🔍 可能的原因

### 1. 网络连接问题
- ❌ 网络不稳定
- ❌ 防火墙阻止连接
- ❌ 代理设置问题
- ❌ DNS解析失败

### 2. AKShare数据源问题
- ❌ 东方财富服务器暂时不可用
- ❌ 数据接口维护中
- ❌ 访问频率限制

### 3. 环境限制
- ❌ Trae环境网络限制
- ❌ 端口被封锁
- ❌ SSL证书问题

## ✅ 解决方案

### 方案1：检查网络连接

#### Windows系统：
```bash
# 测试网络连接
ping eastmoney.com

# 测试端口连接
telnet www.eastmoney.com 80
```

#### 检查防火墙：
1. 打开"Windows Defender 防火墙"
2. 点击"允许应用通过防火墙"
3. 确保Python有网络访问权限

### 方案2：使用代理（如果需要）

```python
# 在代码中添加代理设置
import akshare as ak
import os

# 设置代理
os.environ['HTTP_PROXY'] = 'http://your-proxy:port'
os.environ['HTTPS_PROXY'] = 'http://your-proxy:port'

# 然后获取数据
df = ak.stock_zh_a_spot_em()
```

### 方案3：使用备用数据源

#### 方法A：使用Tushare
```python
import tushare as ts

# 设置token
ts.set_token('your_token_here')
pro = ts.pro_api()

# 获取股票列表
df = pro.stock_basic(exchange='', list_status='L')
```

#### 方法B：使用Baostock
```python
import baostock as bs

# 登录系统
lg = bs.login()

# 获取股票列表
rs = bs.query_all_stock(day="2024-01-01")
```

#### 方法C：手动输入数据
创建一个CSV文件，手动输入股票数据：

```csv
股票代码,股票名称,最新收盘价,2024年每股分红,2025年归母净利润增长率,近三年股利支付率
600000,浦发银行,7.8,0.51,4.5,30
601318,中国平安,45,2.34,7.8,45
...
```

### 方案4：使用Google Colab（推荐）

Google Colab有更好的网络环境：

1. 访问 https://colab.research.google.com/
2. 新建Notebook
3. 运行以下代码：

```python
# 安装AKShare
!pip install akshare

# 导入库
import akshare as ak

# 获取数据
df = ak.stock_zh_a_spot_em()
print(f"获取到 {len(df)} 只股票")
```

### 方案5：稍后重试

网络问题可能是暂时的，可以：

1. **等待5-10分钟后重试**
2. **更换网络环境**（如从WiFi切换到4G）
3. **重启网络设备**

## 🛠️ 健壮版本的使用

我已经创建了 `高股息股票筛选_健壮版.py`，包含：

### 特点：
1. ✅ **自动重试机制**：失败后自动重试3次
2. ✅ **指数退避**：每次重试延迟时间翻倍
3. ✅ **错误提示**：详细说明失败原因
4. ✅ **进度显示**：显示成功/失败数量

### 运行方法：
```bash
python "高股息\高股息股票筛选_健壮版.py"
```

## 📊 备用方案：使用静态数据

如果网络问题持续存在，可以：

1. **从东方财富网站手动下载数据**
   - 访问：http://quote.eastmoney.com/
   - 导出股票列表

2. **从同花顺网站获取数据**
   - 访问：https://www.10jqka.com.cn/
   - 导出财务数据

3. **使用Excel手动整理数据**
   - 创建CSV文件
   - 按照格式输入数据

## 🎯 推荐方案

### 短期解决方案：
1. **使用健壮版本**（包含重试）
2. **稍后重试**（等待网络恢复）
3. **使用Google Colab**（网络更稳定）

### 长期解决方案：
1. **在本地Python环境运行**（网络更稳定）
2. **使用备用数据源**（Tushare、Baostock等）
3. **建立本地数据库**（缓存历史数据）

## 🔧 代码修改建议

如果网络问题持续，可以修改代码：

### 添加超时设置：
```python
import akshare as ak
import requests

# 设置超时
ak.set_timeout(30)

# 获取数据
df = ak.stock_zh_a_spot_em()
```

### 添加缓存机制：
```python
import pickle
import os
from datetime import datetime, timedelta

# 缓存文件路径
cache_file = 'stock_data_cache.pkl'

# 检查缓存是否过期
if os.path.exists(cache_file):
    cache_time = datetime.fromtimestamp(os.path.getmtime(cache_file))
    if datetime.now() - cache_time < timedelta(hours=1):
        # 使用缓存数据
        with open(cache_file, 'rb') as f:
            df = pickle.load(f)
    else:
        # 重新获取数据
        df = ak.stock_zh_a_spot_em()
        with open(cache_file, 'wb') as f:
            pickle.dump(df, f)
else:
    # 获取新数据
    df = ak.stock_zh_a_spot_em()
    with open(cache_file, 'wb') as f:
        pickle.dump(df, f)
```

## 📞 技术支持

如果问题持续存在：

1. **检查AKShare官方文档**
   - GitHub: https://github.com/akfamily/akshare
   - 文档: https://akshare.akfamily.xyz/

2. **查看东方财富状态**
   - 官网: http://www.eastmoney.com/
   - 检查是否有维护公告

3. **联系网络管理员**
   - 检查防火墙设置
   - 确认端口是否开放

## 📝 总结

### 当前问题：
- ❌ 网络连接被拒绝
- ❌ 无法获取实时数据

### 立即行动：
1. ✅ 使用健壮版本（自动重试）
2. ✅ 检查网络连接
3. ✅ 稍后重试

### 备用方案：
1. ✅ 使用Google Colab
2. ✅ 使用备用数据源
3. ✅ 手动输入数据

### 最佳实践：
- ✅ 在本地环境运行（网络更稳定）
- ✅ 使用缓存机制（减少网络请求）
- ✅ 建立备用数据源（提高可靠性）

**网络问题通常是暂时的，请稍后重试！**
